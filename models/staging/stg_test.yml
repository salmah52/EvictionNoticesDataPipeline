## Testing, documentation, Referencing and configuration
version: 2

models:
  - name: mart.dim_location
    description: " Table describing and detailing locations"
    columns:
      - name: location_id
        description: "The order Id of the order"
        tests:
  
          - unique
